{{- $defaultConfigs := .Values.station.defaultConfigs | default dict }}
{{- if ne ($defaultConfigs.type | default "configmap") "hostpath" }}
{{- $confFiles := .Files.Glob "default-configs/conf/**" }}
{{- $connectorFiles := .Files.Glob "default-configs/connectors/**" }}
{{- $topologyFiles := .Files.Glob "default-configs/topology/**" }}
{{- $modelFiles := .Files.Glob "default-configs/models/**" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "station.defaultConfigsName" . }}
  labels:
    {{- include "station.labels" . | nindent 4 }}
data:
{{- range $path, $_ := $confFiles }}
  {{ trimPrefix "default-configs/" $path | replace "/" "__" }}: |-
{{ $.Files.Get $path | indent 4 }}
{{- end }}
{{- range $path, $_ := $connectorFiles }}
  {{ trimPrefix "default-configs/" $path | replace "/" "__" }}: |-
{{ $.Files.Get $path | indent 4 }}
{{- end }}
{{- range $path, $_ := $topologyFiles }}
  {{ trimPrefix "default-configs/" $path | replace "/" "__" }}: |-
{{ $.Files.Get $path | indent 4 }}
{{- end }}
{{- range $path, $_ := $modelFiles }}
  {{ trimPrefix "default-configs/" $path | replace "/" "__" }}: |-
{{ $.Files.Get $path | indent 4 }}
{{- end }}
{{- end }}
