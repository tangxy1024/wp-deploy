apiVersion: v1
kind: Service
metadata:
  name: {{ include "wparse.fullname" . }}-admin
  labels:
    {{- include "wparse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.adminService.type }}
  ports:
    - name: admin
      port: {{ .Values.adminService.port }}
      targetPort: admin
      protocol: TCP
      {{- if and (eq .Values.adminService.type "NodePort") .Values.adminService.nodePort }}
      nodePort: {{ .Values.adminService.nodePort }}
      {{- end }}
  selector:
    {{- include "wparse.selectorLabels" . | nindent 4 }}
