apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "wparse.fullname" . }}
  labels:
    {{- include "wparse.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "wparse.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- if or (and .Values.warpParseSecret.create (not .Values.warpParseSecret.existingSecret)) .Values.podAnnotations }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- if and .Values.warpParseSecret.create (not .Values.warpParseSecret.existingSecret) }}
        checksum/warp-parse-secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- end }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      labels:
        {{- include "wparse.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "wparse.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: init-config
          image: "{{ .Values.initImage.repository }}:{{ .Values.initImage.tag }}"
          imagePullPolicy: {{ .Values.initImage.pullPolicy }}
          command:
            - sh
            - -c
            - cp -a /config-src/. {{ .Values.wparse.workDir }}/ && mkdir -p {{ .Values.wparse.workDir }}/data/rescue {{ .Values.wparse.workDir }}/data/logs {{ .Values.wparse.workDir }}/data/out_dat {{ .Values.wparse.workDir }}/.run
          volumeMounts:
            - name: config-source
              mountPath: /config-src
              readOnly: true
            - name: config
              mountPath: {{ .Values.wparse.workDir }}
      containers:
        - name: {{ .Chart.Name }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.command }}
          command:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.args }}
          args:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if or .Values.timezone.enabled .Values.env }}
          env:
            {{- if .Values.timezone.enabled }}
            - name: TZ
              value: {{ .Values.timezone.name | quote }}
            {{- end }}
            {{- with .Values.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- end }}
          {{- with .Values.envFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            {{- range .Values.service.ports }}
            - name: {{ .name }}
              containerPort: {{ .containerPort | default .port }}
              protocol: {{ .protocol | default "TCP" }}
            {{- end }}
            - name: admin
              containerPort: {{ .Values.adminService.port }}
              protocol: TCP
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: config
              mountPath: {{ .Values.wparse.workDir }}
            - name: warp-parse-secret
              mountPath: {{ .Values.wparse.warpParseSecretPath }}
              readOnly: true
            {{- with .Values.volumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        - name: config-source
          configMap:
            name: {{ include "wparse.fullname" . }}-config
            items:
              {{- range $path, $_ := .Files.Glob "wparse-config/**" }}
              - key: {{ trimPrefix "wparse-config/" $path | replace "/" "__" }}
                path: {{ trimPrefix "wparse-config/" $path }}
              {{- end }}
        - name: config
          emptyDir: {}
        - name: warp-parse-secret
          secret:
            secretName: {{ include "wparse.warpParseSecretName" . }}
            defaultMode: 0400
            items:
              - key: admin_api.token
                path: admin_api.token
              {{- if .Values.warpParseSecret.tls.enabled }}
              - key: tls.crt
                path: tls/server.crt
              - key: tls.key
                path: tls/server.key
              {{- end }}
      {{- with .Values.volumes }}
      {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
