{{- if .Values.wparse.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "wparse.fullname" . }}
  labels:
    {{- include "wparse.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "wparse.fullname" . }}
  minReplicas: {{ .Values.wparse.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.wparse.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.wparse.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.wparse.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.wparse.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.wparse.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
