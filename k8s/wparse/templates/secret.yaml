{{- if and .Values.wparse.warpParseSecret.create (not .Values.wparse.warpParseSecret.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "wparse.warpParseSecretName" . }}
  labels:
    {{- include "wparse.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- $token := .Files.Get .Values.wparse.warpParseSecret.tokenPath }}
  {{- if not $token }}
  {{- fail (printf "admin token file not found or empty: %s" .Values.wparse.warpParseSecret.tokenPath) }}
  {{- end }}
  admin_api.token: |-
{{ $token | indent 4 }}
  {{- if .Values.wparse.warpParseSecret.tls.enabled }}
  {{- $crt := .Files.Get .Values.wparse.warpParseSecret.tls.crtPath }}
  {{- $key := .Files.Get .Values.wparse.warpParseSecret.tls.keyPath }}
  {{- if not $crt }}
  {{- fail (printf "TLS cert file not found or empty: %s" .Values.wparse.warpParseSecret.tls.crtPath) }}
  {{- end }}
  {{- if not $key }}
  {{- fail (printf "TLS key file not found or empty: %s" .Values.wparse.warpParseSecret.tls.keyPath) }}
  {{- end }}
  tls.crt: |-
{{ $crt | indent 4 }}
  tls.key: |-
{{ $key | indent 4 }}
  {{- end }}
{{- end }}
